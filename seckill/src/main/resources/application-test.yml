server:
  port: 9090

spring:
  datasource:
    name: seckill
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.life.mysql.dao

seckill:
  enable: 1
